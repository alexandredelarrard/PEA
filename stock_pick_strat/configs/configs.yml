data_mode: remote
seed: 4325

data_extract:
  years_history: 15
  benchmark_ticker: SPY
  other_tickers: ["SPY", "^VIX", "CL=F", "GC=F", "USDEUR=X"]
  llm_model: "gpt-5.6-terra"
  sec:
    # SEC EDGAR requires a descriptive User-Agent — set SEC_USER_AGENT in .env
    form_types: ["10-K", "10-Q", "8-K"]
    rate_limit_seconds: 0.15