[project]
name = "stock-pick-strat"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit (>=1.35)",
    "yfinance (>=0.2.40)",
    "pandas (>=2.2)",
    "numpy (>=1.26, <2.5)",
    "plotly (>=5.22)",
    "fredapi (>=0.5.2)",
    "requests (>=2.32)",
    "lxml (>=5.2)",
    "python-dotenv (>=1.0)",
    "pyarrow (>=16.1)",
    "tqdm (>=4.66)",
    "scipy (>=1.11)",
    "lightgbm (>=4.0)",
    "omegaconf (>=2.3)",
    "matplotlib (>=3.8)",
    "openpyxl (>=3.1)",
    "openai (>=2.45.0,<3.0.0)",
    "pytrends (>=4.9.2,<5.0.0)",
    "curl-cffi (>=0.7,<1.0)",
    "sqlalchemy (>=2.0,<3.0)",
    "psycopg2-binary (>=2.9,<3.0)",
    # Local earnings-call sentiment (FinBERT-tone). CPU wheel installs from PyPI; for GPU
    # reinstall the CUDA build: pip install torch --index-url https://download.pytorch.org/whl/cu124
    "torch (>=2.6)",
    "transformers (>=4.40,<5.0)"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
