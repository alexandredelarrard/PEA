this daemon is inside a Windows job object that terminates its members when it closes, so it will be killed the moment the tool that launched it exits — silently, with no shutdown line and no exit code, because a terminated process cannot write one. Detaching does not escape a job object. If vexp seems to vanish seconds after reporting its socket, this is why: start it from an ordinary terminal instead, and it will outlive the session.