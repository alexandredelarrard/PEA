data_mode: remote
seed: 4325

data_extract:
  years_history: 15
  # `prices_macro`'s only window. Deeper than years_history on purpose: the risk-parity and
  # trend sleeves backtest through the dot-com and GFC drawdowns. The SERIES LIST is not a
  # config key -- it cannot be separated from its symbol->name mapping without creating two
  # places to edit, so it lives in constants.MACRO_PRICE_SERIES / MACRO_FRED_SERIES.
  macro_years_history: 31
  notes_years_history: 15
  redundant_ticks: ["GOOG", "FOX", "NWS", "EA"] #EA got acquired
  llm_model: "gpt-5-mini"
  # Self-heal cadence for the EDGAR filing-listing fetchers (13D, 8-K, DEF 14A
  # edgar + LLM, fundamentals edgartools): each uses the extraction manifest's
  manifest_full_rescan_days: 30
